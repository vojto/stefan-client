# echo "Building"
# hem build
# git add .
# git commit -m "Building"
# git push
# ssh vojto@rinik.net "cd /var/www/apps/stefan && git pull"

hem build
scp -r ./public/* vojto@stefan.dev.cnl.sk:/var/www